<h5 class="burgerName">{{burger_name}}</h5>

<div class="burgerList">
	<button class="change-devoured waves-effect waves-light btn-large" data-id="{{dataValues.id}}" data-newdevour="{{devour}}">
		{{#if devour}}Order{{else}}Yum! Order Again!{{/if}}
	</button>

	<button class="delete-burger btn-floating btn-large waves-effect waves-light red" data-id="{{dataValues.id}}"><i class="large material-icons">delete</i>Remove
		from Menu</button>
	<br>
	<br>
	<hr>

</div>